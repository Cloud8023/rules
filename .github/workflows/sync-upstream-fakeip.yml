name: 同步上游 clash-rules release 分支
on:
  schedule:
    # 每天北京时间 9:25 执行（UTC 01:25）
    - cron: '25 1 * * *'
  workflow_dispatch:

jobs:
  sync-fakeip:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 检出代码
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: fakeip

      - name: 配置 Git 身份
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 拉取上游并同步
        run: |
          git remote add upstream https://github.com/wwqgtxx/clash-rules.git
          
          # 关键1：只拉取分支，不拉取任何标签（Tag）
          git fetch upstream --no-tags
          
          git reset --hard upstream/release
          
          # 关键2：删除上游自带的工作流目录，阻止自动发布脚本运行
          rm -rf .github
          
          # 有变更才提交，避免空提交
          if [ -n "$(git status --porcelain)" ]; then
            git add -A
            git commit -m "sync: 同步上游 clash-rules 规则"
            # 关键3：推送时也不同步标签
            git push origin fakeip --force --no-tags
          else
            echo "上游无更新，跳过推送"
          fi
